services:
  - type: web
    name: voxpulse
    env: docker
    dockerfilePath: Dockerfile
    plan: starter

    envVars:
      - key: HF_TOKEN
        sync: false

      - key: PYTHONUNBUFFERED
        value: "true"

      - key: PORT
        value: "7860"